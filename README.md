# TornilloFeliz - Expo (TypeScript)
Proyecto Expo + TypeScript minimal para una ferretería (Tornillo Feliz).
Pasos para ejecutar:
1. `npm install`
2. `npx expo start`
Requisitos:
- Node.js (14+), npm
- Expo CLI (opcional)
